sources:
  - name: jaffle_shop
    description: working with jaffle_shop schema tables
    database: raw  
    schema: jaffle_shop  
    tables:
      - name: orders
        description: tacking the orders transactions
        freshness:
          warn_after:
            count: 10
            period: day
          error_after:
            count: 20
            period: day
        loaded_at_field: _ETL_LOADED_AT
      - name: customers
        description: tracking customers dats
